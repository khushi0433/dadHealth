const DHBadge = () => (
  <div className="w-9 h-9 bg-background border-2 border-foreground flex items-center justify-center shrink-0">
    <span className="font-heading text-sm font-extrabold text-foreground tracking-wide">DH</span>
  </div>
);

export default DHBadge;
